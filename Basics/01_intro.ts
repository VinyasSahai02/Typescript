//ts is a development tool like a wrapper around js
//the project still runs in js
//ts gets converted to js-->TypeScript is transpiled into JavaScript using a compiler.

//ts checks if the specified type matches before running the code

const user={name:"vinyas",age:21}
console.log("Sahai");
// console.log(user.email);
export{}

//diff types include--> any,unknown,object,void,undefined,null,never
// see docs for better understanding
